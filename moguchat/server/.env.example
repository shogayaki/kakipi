# Claude API キー（サーバー側のみで保持する。クライアントには絶対に置かない）
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxx

# 使用モデル（任意）。デフォルトは claude-opus-4-8。
# 無課金ユーザー中心でコストを抑えたい場合は claude-haiku-4-5 / claude-sonnet-4-6 などに変更。
# MODEL=claude-haiku-4-5

# ポート（任意、デフォルト 8787）
# PORT=8787
